server:
  port: 83


spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos服务器地址


#消费者将要去访问的微服务名称(注册成功进nacos的微服务提供者) 该配置可配可不配
service-url:
  nacos-user-service: http://nacos-payment-provider # 去访问的服务地址


